@model IEnumerable<identity1.Models.Commander>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CommanderLogin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CommanderPass)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CommanderLogin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CommanderPass)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CommanderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CommanderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CommanderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
